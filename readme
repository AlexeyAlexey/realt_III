Get HTML page, parse them pages and paste in DB reference (new apartment).

treeHashII.rb

hash1 = {:value1 => :one, value2 => :two}
hash2 = {:one => 1, :ten => 10}

hash1.treeHash hash2 #=> {:value1 => 1, value2 => :two}

This app was deployed to Heroku
